extends layout.pug

block content
  h2 New Book

  include error

  form(action="/books/new", method="post")
    include form
    p
      input(
        type='submit'
        value='Create New Book'
        aria-label='create new book'
        )
    p
      a(aria-label='cancel' class='button' href='/books') Cancel
